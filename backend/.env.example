# --- App ---
SECRET_KEY=CHANGE_ME_TO_A_LONG_RANDOM_STRING
ACCESS_TOKEN_EXPIRE_MINUTES=60
PROJECT_NAME='Service de vélo libre-service'
API_VERSION=0.1.0
PORT=8080

# --- DB (matches docker-compose) ---
MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_DATABASE=apidb
MYSQL_USER=apiuser
MYSQL_PASSWORD=apipassword

# --- SQLAlchemy URL (points to the db service name) ---
# Use the MariaDB dialect to avoid version parsing quirks.
DATABASE_URL=mariadb+pymysql://apiuser:apipassword@db:3306/apidb?charset=utf8mb4
